name: Test Local Action Outside Standard GitHub Workflows Directory
on: [push]

jobs:
  test:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout code
        uses: actions/checkout@v5
      - name: Test Save Changes Action
        uses: ./
        with:
          github_token: "test-github-token"
          message: 'Save changes'
          push_changes: 'false'